<%= form_for @keywords, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :titre, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :titre, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :assigned_articles, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :assigned_articles, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  keywords_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
